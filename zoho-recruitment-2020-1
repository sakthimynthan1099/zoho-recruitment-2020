#include <stdio.h>
#include <string.h>
int main() {
  int i,j,n;
  char s[]="water",b[100];
 // scanf("%s",s[i]);
  for(i=0;i<=strlen(s);i++)
  {
      for(j=i;j<=strlen(s);j++)
     {
     
    printf("%c",s[j]);
     }
     printf("\n");
  }  
    return 0;
}
